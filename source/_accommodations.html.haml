.accommodations
  %section
    %h2 Hotels

    %p.large-paragraph
      Discounted rates are available at two Glastonbury hotels. The hotels are
      conveniently located next to each other.

    = image_tag "hotels.png", class: "accomodation-hero block-image"

    - data.accommodations.each do |a|
      .accommodation
        %h5.header-divider= a.name
        %p= a.description
        %ul
          %li
            = link_to a.directions, target: "_blank" do
              = a.street_address
              %br
              = a.extended_address
          %li Phone: #{a.phone}
        = link_to "Book Now", a.booking_url, target: "_blank", class: "button"
